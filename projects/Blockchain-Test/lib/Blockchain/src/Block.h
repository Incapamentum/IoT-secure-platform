#ifndef BLOCK_H
#define BLOCK_H

#include "Transactions.h"

// Block contains a set (i.e. array) of digitally signed
// transactions, along with a previous hash + a nonce
// value (proof-of-work)
class Block
{
    
};

#endif